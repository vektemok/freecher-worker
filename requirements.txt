# One source of truth for dependencies: pyproject.toml.
#
# Installing this file and installing the project are the same operation, so
# the two can never drift apart the way they did when boto3 and yt-dlp were
# added to the project but not here.
#
#   pip install -r requirements.txt
#
# ffmpeg/ffprobe are not Python packages and must be installed separately
# (brew install ffmpeg / apt-get install ffmpeg).
-e .[dev]
